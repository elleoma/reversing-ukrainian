## part 20 - обробка коду інструкції

Для повного змісту всіх уроків, будь ласка, натисніть нижче, оскільки він дасть короткий короткий урок на додаток до тем, які він висвітлює.&nbsp;https://github.com/mytechnotalent/Reverse-Engineering-підручник

Процесор зчитує коди інструкцій, які зберігаються в пам'яті, оскільки кожен набір коду може містити один з інших байт інформації, які спрямовують процесор для виконання дуже конкретного завдання. Коли кожен код інструкції читається з пам'яті, будь -які дані, необхідні для коду інструкції, також зберігаються and, прочитані в пам'яті. Майте на увазі, що пам'ять, що містять коди інструкцій, не відрізняються від байтів, які містять дані, які використовуються спеціальними вказівниками CPU and, щоб допомогти процесору відстежувати, де в даних пам'яті є and, де зберігаються коди інструкцій. Покажчик даних допомагає процесору відстежувати, куди починається область даних у пам'яті, що є стеком. Коли нові елементи даних розміщуються в стеку, вказівник стека рухається вниз у пам'яті and, коли дані читаються зі стека, вказівник стека рухається в пам'яті. Перегляньте частину 15 - Стек, якщо ви не розумієте цю концепцію. Покажчик інструкцій використовується для того, щоб допомогти процесору відстежувати, які коди інструкцій вже обробляються and Який код повинен бути оброблений далі. Перегляньте частину 12 - Реєстр покажчиків інструкцій, якщо ви не розумієте цю концепцію. Кожен and кожен код інструкції повинен включати опкод, який визначає основну функцію or, що виконується за допомогою процесора, до якого опкоди є між 1 and 3 байтами довжиною and унікально визначають функцію, яка виконується. Давайте розглянемо просту програму C під назвою test.C для початку. <div class="slate-resizable-image-embed slate-image-embed__resize-middle"><img src="/imgs/1520446165914.jpg"/></div>

Все, що ми робимо, - це створення функції main типу цілого числа, до якого він має порожній параметр and, що повертається 0. Вся ця програма - це просто exit os. Давайте складати and запустити цю програму. <div class="slate-resizable-image-embed slate-image-embed__resize-full-width"><img src="/imgs/1520147571659.jpg"/></div>

Дозволяє використовувати інструмент objdump для and Знайдіть функцію main всередині нього. <div class="slate-resizable-image-embed slate-image-embed__resize-full-width"><img src="/imgs/1520190277217.jpg"/></div>

Ось фрагмент результатів, які ви отримаєте, запустивши вищезазначену команду.&nbsp;, це вміст функції main.&nbsp;keeke на увазі, що нижче є в Intel Syntax, коли ми говорили в останньому підручник. <div class="slate-resizable-image-embed slate-image-embed__resize-middle"><img src="/imgs/1520446166055.jpg"/></div>

У лівій відстань у нас є відповідні адреси пам'яті. У центрі ми маємо Opcodes and, нарешті, праворуч ми маємо відповідну мову складання в синтаксисі Intel. Щоб зберегти це просто, давайте вивчити адресу пам'яті __80483De__, де ми бачимо коди OP __B8 00 00 00 00__. Ми бачимо, що опкод __B8__ відповідає __ -мові eax, 0x0__ інструкції праворуч. Наступна серія 00 00 00 00 являє собою 4 байти значення 0. Майте на увазі, що платформа IA-32 використовує те, що ми call маленький ендіанський позначення, що означає, що байти нижньої вартості з’являються спочатку в порядку, читаючи праворуч зліва. Я хочу переконатися, що у вас це прямо в голові, щоб зробити вигляд, що значення вище було:

__mov eax, 0x1__

У цьому сценарії відповідним опкодом буде:

__B8 01 00 00__

Якщо ви розгублені, це нормально. Пам'ятаєте маленький ендіан? Майте на увазі eax шириною 32 біт, тому це 4 байти (8 біт = 1 байт). Значення перераховані у зворотному порядку, тому ми бачимо вищезазначене зображення. Я з нетерпінням чекаю зустрічі з вами на наступному тижні, коли ми занурюємось у деталі про те, як скласти програму.